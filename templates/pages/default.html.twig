{% extends 'base.html.twig' %}

{% block content %}
    {% include "components/header-image.html.twig" %}
    
    <div class="py-16 bg-white lg:w-[1200px] m-auto">
        <div class="container m-auto px-6 text-gray-600 md:px-12 xl:px-6">
            <h1>{{ content.title }}</h1>

            {% if content.article %}
                <div class="mt-8">
                    {{ content.article|raw }}
                </div>
            {% endif %}

            {% if content.title == "Datenschutzerklärung" %}
                <div id="matomo-opt-out"></div><script src="https://matomo.sreisueber.uk/index.php?module=CoreAdminHome&action=optOutJS&divId=matomo-opt-out&language=auto&showIntro=1"></script>
            {% endif %}
        </div>
    </div>
{% endblock %}
