<div class="py-16 bg-white w-full max-w-[1200px] m-auto">
    <div class="container m-auto px-6 text-gray-600 md:px-12 xl:px-6">
        <div class="space-y-6 md:space-y-0 md:flex md:gap-6 lg:items-center lg:gap-12">
            <div class="md:5/12 lg:w-3/12">
                {% if content.about_me_image %}
                    <img src="{{ content.about_me_image.url }}"
                         alt="Fotografin Sandra Reisüber"
                         loading="lazy"
                         class="rounded-full w-60 h-60 object-cover">
                {% endif %}
            </div>
            <div class="md:7/12 lg:w-8/12">
                <h2 class="text-5xl md:text-6xl text-primary">Eure Fotografin</h2>
                <div class="mt-6 text-gray-600">
                    {{ content.about_me_text | raw }}
                </div>
            </div>
        </div>
    </div>
</div>