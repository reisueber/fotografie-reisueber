{% if content.header_image %}
    <div
            class="mx-auto h-[40rem] w-full overflow-y-scroll bg-cover bg-fixed bg-top bg-no-repeat shadow-lg"
            style="background-image:url({{ content.header_image.url }});">
        <div class="container mx-auto h-full flex items-center justify-center">
            <h2 class="text-6xl text-white text-center">Augenblicke für die Ewigkeit</h2>
        </div>
    </div>
{% endif %}